# JCAP final citation update checklist

The bibliography key to update is `palominoylla2026ringdown` in `paper/journal_identifiability_final/references.bib`. This was Ref. 23 before the cleanup and is Ref. 25 in the current compiled PDF; use the stable key rather than the transient number. Until official publisher metadata is available, the verified status remains “Accepted for publication in JCAP.”

Verify from the publisher before changing the entry:

- final title;
- journal name;
- year;
- volume;
- article number;
- DOI;
- final author list;
- publication date, if included.

Then recompile, rerun the reference audit, confirm the novelty-boundary wording matches the final title, and create the submission package. No placeholder DOI, volume, or article number should appear.
