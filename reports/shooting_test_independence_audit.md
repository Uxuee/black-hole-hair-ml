# Shooting test independence audit

Constraint conservation, observer-plane hits, continuation smoothness, and finite-difference checks of a Hamiltonian against its own implementation are **SELF-CONSISTENCY ONLY**. They are useful numerical tests but cannot prove that the Hamiltonian is physical. Several Schwarzschild limit and `k=0` comparisons are **PARTIALLY INDEPENDENT** because expected relations are analytic, while some expected values reuse package formulas. CSV loader/schema tests are **INSUFFICIENT** for physics.

The new frozen-reference suite is **INDEPENDENT**: expected numbers were generated by the separate notebook derivation, stored literally with provenance, and are not generated from production functions at test runtime. Existing tests were not deleted or relabeled in code.

Verdict: **PASS_WITH_WARNING**. No existing tautological test caused a false physics verdict here, but small Hamiltonian residuals and successful shooting must never be cited alone as validation.
